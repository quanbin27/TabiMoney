@startuml ACT_008_CreateGoal
start
:User truy cập trang quản lý mục tiêu;
:User nhấn "Tạo mục tiêu mới";
:User nhập thông tin:
title, description, target_amount,
target_date, goal_type, priority;
:Validate dữ liệu;
if (Target amount <= 0?) then (có)
  :Trả về lỗi "Target amount must be greater than 0";
  stop
endif
if (Target date trong quá khứ?) then (có)
  :Cảnh báo nhưng vẫn cho phép tạo;
endif
:Tạo financial_goal record;
:Tính toán progress (current_amount / target_amount);
:Trả về goal đã tạo;
:Hiển thị mục tiêu mới trong danh sách;
stop
@enduml

