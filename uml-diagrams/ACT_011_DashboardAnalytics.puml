@startuml ACT_011_DashboardAnalytics
start
:User truy cập trang Dashboard;
:Kiểm tra cache Redis;
if (Có cache và chưa hết hạn?) then (có)
  :Trả về dữ liệu từ cache;
  :Hiển thị dashboard;
  stop
endif
:Query database để tính toán:
- Total income tháng này
- Total expense tháng này
- Net savings (income - expense)
- Spending by category (pie chart data)
- Spending trends (line chart data)
- Top categories
- Budget status;
if (Không có dữ liệu?) then (có)
  :Trả về dữ liệu mặc định (0 cho tất cả);
  :Hiển thị "Chưa có dữ liệu";
  stop
endif
:Lưu kết quả vào cache (TTL: 5 phút);
:Trả về analytics data;
:Frontend hiển thị biểu đồ và số liệu;
stop
@enduml

